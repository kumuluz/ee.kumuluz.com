<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>{{groupId}}</groupId>
    <artifactId>{{artifactId}}</artifactId>
    <version>{{projectVersion}}</version>
    <packaging>jar</packaging>

    {{ #isName }}
    <name>{{name}}</name>
    {{ /isName }}
    {{ #isDescription }}
    <description>{{description}}</description>
    {{ /isDescription }}

    <properties>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <kumuluz.version>{{kumuluzVersion}}</kumuluz.version>
        {{ #isConfig }}
        <kumuluzee-config.version>{{kumuluzeeConfigVersion}}</kumuluzee-config.version>
        {{ /isConfig }}
        {{ #isLogs }}
        <kumuluzee-logs.version>{{kumuluzeeLogsVersion}}</kumuluzee-logs.version>
        {{ /isLogs }}
        {{ #isDiscovery }}
        <kumuluzee-discovery.version>{{kumuluzeeDiscoveryVersion}}</kumuluzee-discovery.version>
        {{ /isDiscovery }}
        {{ #isStreaming }}
        <kumuluzee-streaming.version>{{kumuluzeeStreamingVersion}}</kumuluzee-streaming.version>
        {{ /isStreaming }}
        {{ #isSecurity }}
        <kumuluzee-security.version>{{kumuluzeeSecurityVersion}}</kumuluzee-security.version>
        {{ /isSecurity }}
	{{ #isREST }}
        <kumuluzee-rest.version>{{kumuluzeeRESTVersion}}</kumuluzee-rest.version>
        {{ /isREST }}
        {{ #isCORS }}
        <kumuluzee-cors.version>{{kumuluzeeCORSVersion}}</kumuluzee-cors.version>
        {{ /isCORS }}
        <maven-dependency-plugin.version>2.10</maven-dependency-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.kumuluz.ee</groupId>
                <artifactId>kumuluzee-bom</artifactId>
                <version>${kumuluz.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        {{ #dependencies }}
        <dependency>
            <groupId>{{ groupId }}</groupId>
            <artifactId>{{ artifactId }}</artifactId>
            {{ #version }}
            <version>{{ version }}</version>
            {{ /version }}
        </dependency>
        {{ /dependencies }}
    </dependencies>

    <build>
        <plugins>
          <plugin>
                <groupId>com.kumuluz.ee</groupId>
                <artifactId>kumuluzee-maven-plugin</artifactId>
                <version>2.4.0-SNAPSHOT</version>
                <executions>
                    {{ ^isUberJAR }}
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                    </execution>
                    {{ /isUberJAR }}
                    {{ #isUberJAR }}
                    <execution>
                        <id>repackage</id>
                        <phase>package</phase>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                    {{ /isUberJAR }}
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
