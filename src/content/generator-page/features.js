import {CheckboxModel, VersionedLabel} from "./schema/checkbox-model.class";

export const featuresList = [
    new CheckboxModel("feat-uberjar", [
        new VersionedLabel("2.2.0", "Generate UBER JAR", "n/a")
    ], [])
];
